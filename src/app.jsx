var React = require('react');
var Routes = require('./routes');
var InfiniteGrid = require('react-infinite-grid');
var Recent = require('./components/recent-games');

React.render(Routes, document.querySelector('.container'));
